"""Refrain — an open description language for clinical neurofeedback protocols.

This is the v0.0 reference implementation. Public API will grow as the runtime
matures; currently exposes only the version string.

See `docs/SPEC.md` for the language reference and `docs/TOUR.md` for an
introduction.
"""

__version__ = "0.0.0"
__all__ = ["__version__"]
