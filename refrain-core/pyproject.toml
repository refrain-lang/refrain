[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "refrain-core"
version = "0.5.0"
description = "Portable Refrain real-time evaluator (PoC) — Python binding"
requires-python = ">=3.10"

[tool.maturin]
features = ["python"]
module-name = "refrain_core"
